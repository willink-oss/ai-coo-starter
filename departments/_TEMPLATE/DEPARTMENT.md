# <Department name>

> Charter for the AI COO when working in this area. Keep it short and current.

## Owns

<What this department is responsible for. e.g. "All customer-facing web properties
and their deploys." Be specific enough that the AI knows when a task belongs here.>

## Rules

- <Area-specific rules the AI must follow. e.g. "Never deploy to production on a
  Friday." / "All copy goes through the style guide before publishing.">
- <Which approval level common actions fall under, if it differs from the default.>

## Outputs

- <Where deliverables go. e.g. "Reports → `departments/<name>/reports/YYYY-MM-DD-*.md`.">

## Live-state checks

- <For status questions in this area, what does the AI measure instead of trusting docs?
  e.g. "Site health → `curl -o /dev/null -s -w '%{http_code}' <url>`.">
